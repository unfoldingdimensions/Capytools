Capytools expansion research is complete (codebase map + market demand + monetization, fully sourced above). Execute in two parts:

**Part 1 — Persist the research (the requested deliverable):**
Write `docs/research/expansion-roadmap.md` containing the full findings, matching the repo's existing `docs/research/` convention: the 3-tool codebase assessment, market thesis evidence, the 9 ranked tool suggestions (CapyStrip, CapyOG, CapyQR, CapyPalette+Contrast, CapyResize+Favicon, CapyToken, CapyCron+Forge, CapyStreak, CapyInvoice) with effort/demand/monetization per tool, the phased monetization roadmap (free → one-time Pro license + Gumroad packs at 10–50K visits → cookieless ads at 50K+ sessions), the traction playbook, and the full source/citation list.

**Part 2 — Pre-flight infra (unblocks everything after):**
1. Commit the already-complete CapyCreator work that is sitting uncommitted (src/app/capycreator/, src/components/tool/CapyCreator.tsx, src/lib/capycreator/, tests/capycreator.test.ts, registration + docs changes) — verify `npm test` passes first.
2. Extract the multi-provider LLM polish layer from `src/lib/capycreator/polish.ts` into shared `src/lib/capytools/llm.ts` (per AGENTS.md's Multi-Provider LLM Polish Standard), keeping CapyCreator's storage key working, with tests still green.

**Not in scope (recommended next build, awaiting your go-ahead after approval):** Tool no. 4 = CapyStrip (EXIF viewer/stripper) — easiest build, strongest brand fit, zero server routes. Recipe: `src/lib/capystrip/` (exif parse + canvas re-encode), `src/components/tool/CapyStrip.tsx`, `src/app/capystrip/page.tsx`, register in both TOOLS arrays, `tests/capystrip.test.ts`.

Validation: `npm test` green after each step; research doc committed on its own commit; no behavior change to existing tools from the polish extraction.