/**
 * The idle state's sample — a paragraph in the house voice that a visitor can
 * load with one click to see the tool work. Counted like any other text; it
 * carries no special status in the engine.
 */
export const DEMO_TEXT = `A quiet tool counts your tokens before anyone bills you for them. Paste the prompt you are about to send, read the exact counts for the encodings that matter, and price them across every model worth comparing — frontier to budget, with the rate card dated so you know how fresh it is. Nothing leaves this tab: no key, no account, no database. The whole calculation runs between your keyboard and your screen, the way small tools should.`;
